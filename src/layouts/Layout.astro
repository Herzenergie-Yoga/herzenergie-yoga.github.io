---
import '../styles/global.css';

interface Props {
	title: string;
}

const { title } = Astro.props;

const navItems = [
  { href: '/', title: 'Home' },
  { href: '/about', title: 'Über mich' },
  { href: '/kurse', title: 'Kurse' },
  { href: '/agb', title: "AGB's & dein Yoga" },
  { href: '/kontakt', title: 'Kontakt' }
];

const currentPath = new URL(Astro.request.url).pathname;
---

<!doctype html>
<html lang="de" class="font-nunito bg-brand-bg text-brand-text scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Herzenergie Yoga - YOGA IN GEORGSMARIENHÜTTE" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Alex+Brush&family=Antic+Didone&family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap" rel="stylesheet">

		<title>{title} | Herzenergie Yoga</title>
	</head>
	<body class="min-h-screen flex flex-col font-light">
    <!-- Header/Nav -->
    <header class="bg-brand-bg sticky top-0 z-50 shadow-sm">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <div class="flex flex-col md:flex-row justify-between items-center gap-6">
          <div class="flex-shrink-0 flex items-center">
            <a href="/" class="text-3xl md:text-4xl font-antic-didone font-light tracking-[0.15em] text-brand-primary hover:text-brand-secondary transition-colors uppercase">
              Herzenergie Yoga
            </a>
          </div>

          <!-- Desktop Navigation -->
          <nav class="hidden md:flex space-x-12">
            {navItems.map(item => (
              <a
                href={item.href}
                class={`text-sm tracking-[0.1em] font-antic-didone font-light transition-colors uppercase
                  ${(currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href)))
                    ? 'text-brand-primary border-b border-brand-primary pb-1'
                    : 'text-brand-text hover:text-brand-primary'
                  }`}
              >
                {item.title}
              </a>
            ))}
          </nav>

          <!-- Mobile menu button -->
          <div class="md:hidden flex items-center w-full justify-center border-t border-brand-secondary/20 pt-4 mt-2">
            <button id="mobile-menu-btn" class="text-brand-text hover:text-brand-primary focus:outline-none p-2 flex items-center gap-2 uppercase tracking-widest text-sm">
              <span>Menü</span>
              <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 6h16M4 12h16M4 18h16"/>
              </svg>
            </button>
          </div>
        </div>
      </div>

      <!-- Mobile Navigation -->
      <div id="mobile-menu" class="hidden md:hidden bg-brand-bg border-b border-brand-secondary/20">
        <div class="px-4 pt-2 pb-6 space-y-4 text-center">
          {navItems.map(item => (
            <a
              href={item.href}
              class={`block px-3 py-2 text-sm uppercase tracking-widest font-antic-didone font-light
                ${(currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href)))
                  ? 'text-brand-primary'
                  : 'text-brand-text hover:text-brand-primary'
                }`}
            >
              {item.title}
            </a>
          ))}
        </div>
      </div>
    </header>

    <!-- Header Image -->
    <div class="w-full h-[35vh] md:h-[45vh] relative overflow-hidden bg-brand-bg">
      <img
        src="/img/header.jpeg"
        alt="Herzenergie Yoga"
        class="w-full h-full object-cover"
      />
    </div>

    <!-- Main Content -->
		<main class="flex-grow max-w-3xl mx-auto w-full px-4 sm:px-6 lg:px-8 py-16 md:py-24">
      <div class="prose prose-lg md:prose-xl prose-stone mx-auto
        prose-headings:font-alex-brush prose-headings:font-normal prose-headings:text-brand-primary prose-headings:text-center
        prose-h1:text-6xl md:prose-h1:text-7xl prose-h1:mb-12
        prose-h2:text-5xl prose-h3:text-4xl
        prose-a:text-brand-secondary prose-a:no-underline hover:prose-a:text-brand-primary hover:prose-a:underline
        prose-p:leading-[1.8] prose-p:text-brand-text prose-p:font-light
        prose-blockquote:border-l-brand-secondary prose-blockquote:bg-brand-bg prose-blockquote:p-6 prose-blockquote:rounded-none prose-blockquote:italic prose-blockquote:text-brand-text prose-blockquote:font-light
        prose-img:rounded-none prose-img:shadow-sm prose-img:mx-auto">
			  <slot />
      </div>
		</main>

    <!-- Footer -->
    <footer class="bg-brand-bg border-t border-brand-secondary/20 mt-auto py-16">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center flex flex-col items-center gap-4">
        <h3 class="text-lg font-antic-didone font-light tracking-[0.2em] text-brand-primary uppercase mb-2">Herzenergie Yoga</h3>
        <p class="text-brand-text font-light tracking-wider text-sm">YOGA IN GEORGSMARIENHÜTTE</p>
        <p class="text-brand-text font-light tracking-wider text-sm">
          <a href="https://herzenergie-yoga.de" class="hover:text-brand-primary transition-colors">herzenergie-yoga.de</a>
        </p>
        <p class="text-brand-text font-light tracking-wider text-sm mb-6">0163-2876265</p>

        <p class="text-sm text-brand-text/80">
          Copyright &copy; Herzenergie Yoga {new Date().getFullYear()}
        </p>
      </div>
    </footer>

    <script>
      // Mobile menu toggle
      const btn = document.getElementById('mobile-menu-btn');
      const menu = document.getElementById('mobile-menu');

      if (btn && menu) {
        btn.addEventListener('click', () => {
          menu.classList.toggle('hidden');
        });
      }
    </script>
	</body>
</html>
